// STUB — replaced by Stream A (A05).
// See tasks/stream-a/A05-skills-view.md.

import { PageShell } from "@/components/layout/page-shell";

export default function SkillsPage() {
  return (
    <PageShell>
      <h1 className="mb-2 text-2xl font-semibold">Skills aprendidas</h1>
      <p className="text-sm text-muted-foreground">TODO: A05 — Domain cards con facts y confidence.</p>
    </PageShell>
  );
}
