// STUB — replaced by Stream A (A07).
// See tasks/stream-a/A07-connected-apps.md.

import { PageShell } from "@/components/layout/page-shell";

export default function ConnectedAppsPage() {
  return (
    <PageShell>
      <h1 className="mb-2 text-2xl font-semibold">Aplicaciones conectadas</h1>
      <p className="text-sm text-muted-foreground">TODO: A07 — listado, scopes, queries, disconnect.</p>
    </PageShell>
  );
}
