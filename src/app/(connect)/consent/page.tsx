// STUB — replaced by Stream C (C04). May be inlined into /connect.
// See tasks/stream-c/C04-consent-screen.md.

import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card";

export default function ConsentPage() {
  return (
    <Card>
      <CardHeader>
        <CardTitle>Consent</CardTitle>
      </CardHeader>
      <CardContent>
        <p className="text-sm text-muted-foreground">TODO: C04 — consent screen UI.</p>
      </CardContent>
    </Card>
  );
}
