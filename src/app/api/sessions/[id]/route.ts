// STUB — replaced by Stream B (B09 / B15).
// See tasks/stream-b/B09-transcript-persist.md and tasks/stream-b/B15-end-screen.md.

import { NextResponse } from "next/server";

export async function GET() {
  return NextResponse.json(
    { error: { code: "internal", message: "Not implemented yet — see B09." } },
    { status: 501 },
  );
}

export async function POST() {
  return NextResponse.json(
    { error: { code: "internal", message: "Not implemented yet — see B09." } },
    { status: 501 },
  );
}
