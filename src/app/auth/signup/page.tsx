// STUB — replaced by Stream A (A02) with real form + signUpAction.
// See tasks/stream-a/A02-signup-page.md.

import { Card, CardContent, CardDescription, CardHeader, CardTitle } from "@/components/ui/card";

export default function SignupPage() {
  return (
    <Card>
      <CardHeader>
        <CardTitle>Crear cuenta</CardTitle>
        <CardDescription>Stub. A02 implements the real form.</CardDescription>
      </CardHeader>
      <CardContent>
        <p className="text-sm text-muted-foreground">TODO: A02 — signup form + server action.</p>
      </CardContent>
    </Card>
  );
}
